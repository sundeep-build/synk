# name: CI

# on:
#   pull_request:
#   push:
#     branches: [main]

# concurrency:
#   group: ci-${{ github.ref }}
#   cancel-in-progress: true

# jobs:
#   analyze-test:
#     runs-on: ubuntu-latest
#     timeout-minutes: 20
#     steps:
#       - uses: actions/checkout@v4
#       - uses: subosito/flutter-action@v2
#         with:
#           flutter-version: 3.41.3
#           channel: stable
#           cache: true
#       - run: flutter pub get
#       - run: dart format --output=none --set-exit-if-changed lib test
#       - run: flutter analyze --fatal-infos
#       - run: flutter test --coverage

#   rules:
#     runs-on: ubuntu-latest
#     timeout-minutes: 10
#     steps:
#       - uses: actions/checkout@v4
#       - uses: actions/setup-java@v4
#         with:
#           distribution: temurin
#           java-version: 21
#       - run: npm i -g firebase-tools
#       # Compiles both rules files in the emulators; any rules error fails the job.
#       - run: firebase emulators:exec --project demo-synk --only firestore,database "bash scripts/check_rules.sh"
#       # Behavioural checks: presence, host control, seq+1 advance, chat rate limits, closed rooms, huddles.
#       - run: firebase emulators:exec --project demo-synk --only database "python3 scripts/rtdb_rules_smoke.py"
#       # Room directory: hosts list their own rooms, only the host ends a room, ended rooms stay ended.
#       - run: firebase emulators:exec --project demo-synk --only firestore "python3 scripts/firestore_rules_smoke.py"
